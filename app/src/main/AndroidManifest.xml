<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appchat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login" android:label="@string/login_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:label="@string/main_name"/>
        <activity android:name=".SingUp" android:label="@string/singup_name"/>
        <activity android:name=".MessageList" android:label="@string/chat_name" >
        </activity>
        <activity android:name=".Info_Account" android:label="@string/Info_name"/>
        <activity android:name=".UpdateInfoAccount" android:label="@string/update_Info_name"/>
        <activity android:name=".ChangePassword" android:label="@string/password_name"/>
    </application>

</manifest>